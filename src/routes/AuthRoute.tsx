import { FC } from "react";
import { Outlet } from "react-router-dom";

//TO DO: Implements Full AuthRoute Component
const AuthRoute: FC = () => {
  return <Outlet />;
};

export default AuthRoute;
